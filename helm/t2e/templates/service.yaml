apiVersion: v1
kind: Service
metadata:
  name: {{ include "t2e.fullname" . }}
  labels:
    {{- include "t2e.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "t2e.selectorLabels" . | nindent 4 }}
